#LOGIN
test % gh auth login
? Where do you use GitHub? GitHub.com
? What is your preferred protocol for Git operations on this host? HTTPS
? Authenticate Git with your GitHub credentials? Yes
? How would you like to authenticate GitHub CLI? Login with a web browser

! First copy your one-time code: xxxx-yyyy
Press Enter to open https://github.com/login/device in your browser... 
✓ Authentication complete.
- gh config set -h github.com git_protocol https
✓ Configured git protocol
✓ Logged in as SreenivasaArasanagatta
*************************************

#creates a local project based on the link provided
%git clone https://github.com/SreenivasaArasanagatta/test.git 

#displays a link to the remote repository
% git remote -v
origin	https://github.com/SreenivasaArasanagatta/test.git (fetch)
origin	https://github.com/SreenivasaArasanagatta/test.git (push)
*******************
#HELP commands
%git <command> - h
%git clone -h
%git remote -h
*******************
#COMMIT -> each commit has a hash key
#create a file
%echo Hello > test.txt

#adds all the file changes to the staging area
% git add .
#allows us to creates a commit
% git commit -m "Initial commit"
[main dbbfa49] Initial commit
 1 file changed, 1 insertion(+)
 create mode 100644 test.txt
#gives overview of commits
%git log
*******************
#STAGING
% echo staged > stagedfile.txt
% echo unstaged >unstagedfile.txt
% git add stagedfile.txt
% git status
On branch main
Your branch is ahead of 'origin/main' by 1 commit.
  (use "git push" to publish your local commits)

Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
	new file:   stagedfile.txt

Untracked files:
  (use "git add <file>..." to include in what will be committed)
	unstagedfile.txt

#Adds one or more files to staging
git add <filenames>

#removes one or more files from the staging
%git reset <filenames>

**********************
#BRANCHES
# A branch is simply a series of commits that can be manipulated independently.
# Git maintains a master branch that is in some sense the source of truth for the project, meaning it is the code that is currently the most upto date.
# ANy commit on a branch can be the origin commit where a new branch can be initiated at that point.

#The command Git branch displays a list of all avaialble branches in your project
% git branch

# to create a new feature branch
% git branch featurebranch
 % git branch              
  featurebranch
* main


% echo master branch > newFileMaster.txt
% git add .
% git commit -m "Master branch commit"
[main ff43c9d] Master branch commit
 3 files changed, 3 insertions(+)
 create mode 100644 newFileMaster.txt
 create mode 100644 stagedfile.txt
 create mode 100644 unstagedfile.txt
arasans@ADSKLYY0JW6Q42 test % 

#command to  CHANGE the BRANCH is git CHECKOUT
% git checkout featurebranch
Switched to branch 'featurebranch'
 % echo feature branch > newfileFeatureBranch.txt
% git add .
 % git commit -m "Feature branch commit"
[featurebranch 7b2a4df] Feature branch commit
 1 file changed, 1 insertion(+)
 create mode 100644 newfileFeatureBranch.txt
arasans@ADSKLYY0JW6Q42 test % 

git branch # displays available branches
git branch <branch name> #creates a new branch
git checkout <branch name> # switches to a different branch
*******************************
# Checking status and inspecting commits
git show <hash>  # gives extensive commit info

git reflog # gives info about all commits on all branches
********************************

#PUSH send files from local repository to the remote repository
# PULL retrieve from remote to the local repository












